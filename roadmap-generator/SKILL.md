---
name: roadmap-generator
description: 多 agent 协作的多编程语言知识点学习内容自动化生成系统。当用户需要学习任意编程语言知识点、生成学习计划、编写测试用例、创建学习文档、制定学习路线、生成面试题时必须使用此技能。支持 Java、Python、Go、PHP 等多种编程语言，包含知识点清单生成、完整性评审、学习文档编写、单元测试生成、测试用例评审、迭代式生成的全流程自动化，支持多 agent 并行执行提高生成效率。
compatibility: 支持多种编程语言，对应语言的构建工具
---

# 多编程语言知识点学习生成器（多 Agent 协作版）

这是一个多 agent 协作的多编程语言学习内容生成系统，能够系统性地生成结构化的编程语言知识点清单、学习文档和配套单元测试用例。支持大规模学习内容的并行生成和进度追踪。

## 核心架构
采用 **规划-执行** 两阶段多 agent 协作模式 + 插件化语言支持：
- **规划阶段**：主 agent 生成整体学习计划，定义任务边界和输出规范
- **执行阶段**：多个子 agent 并行执行具体的文档编写和测试生成任务
- **协调层**：主 agent 负责任务分发、结果汇总、质量检查和进度追踪
- **语言插件层**：支持多种编程语言的插件化扩展，每种语言独立配置

## 支持的编程语言
- Java (8+)
- Python (3.10+)
- Go (1.21+)
- PHP (8.0+)
- 可扩展支持其他语言

## 配置选项
- `output-dir: ./learning/` - 用于存储学习计划和输出文档的默认目录
- `parallel-agents: 3` - 并行执行任务的子 agent 数量
- `auto-execution: disabled` - 是否自动执行计划（默认需要用户确认）
- `target-language: java` - 目标编程语言，可选值：java, python, go, php
- `learning-path: complete` - 学习路径，可选值：complete (从入门到大师), basic (基础), advanced (高级), interview (面试突击)

## 模式 1：规划模式（新建或更新学习计划时）
当用户要求创建学习计划、更新计划，或者要求生成学习内容但输出目录下尚未存在计划文件时，进入此模式。

### 步骤 1.1：需求分析与领域建模
理解用户的学习需求和目标：
1. 确定目标编程语言（Java/Python/Go/PHP等）
2. 确定学习领域（基础、核心类库、Web开发、并发编程等）
3. 评估学习者水平（入门、中级、高级）
4. 明确学习目标（面试准备、项目开发、技能提升等）
5. 估算可用学习时间，合理规划内容量
6. 默认提供 **从入门到大师** 的完整学习路径，需用户确认后开始执行

### 步骤 1.2：设计分阶段学习计划
创建结构化的分阶段学习计划，将知识点组织成逻辑单元，支持五级粒度划分：
- 按学习路径排序：从基础到高级，从理论到实践
- 按五级粒度组织：阶段 → 模块 → 章节 → 知识点 → 子知识点（可选）
- 粒度适配原则：
  - 入门学习：使用 3 级结构（阶段 → 模块 → 知识点）
  - 中高级/面试学习：使用完整 5 级结构
  - 简单知识点可省略中间层级，避免过度拆分
- 每个知识点预计学习时间 30-60 分钟，子知识点 10-20 分钟
- 参考对应语言插件中的标准阶段划分模板

### 步骤 1.3：生成计划文件
在输出目录中创建两个核心计划文件：
1. **学习计划主体**：`@output/plan-learning.md`
   - 完整的知识点清单，分阶段组织
   - 每个知识点的难度、预计学习时间、学习目标
   - 文档编写规范和输出格式要求
   
2. **进度追踪文件**：`@output/plan-progress.md`
   必须包含以下 Metadata 部分：
   ```markdown
   ## Metadata
   - Plan ID: [唯一生成的ID]
   - Target Language: [目标编程语言]
   - Learning Path: [学习路径类型]
   - Last updated: [时间戳]
   - Status: 待确认
   - Current phase: [当前执行的阶段编号]
   - Last completed task: [None]
   - Auto-execution: disabled
   - Parallel agents: 3
   - Adjustment requests: []
   ```
   
   必须包含进度追踪表格：
   ```markdown
   | 阶段 | 模块 | 章节 | 知识点 | 子知识点 | 状态 | 完成度 | 负责人 | 备注 |
   |------|------|------|--------|----------|------|--------|--------|------|
   | 第1阶段：[语言]基础 | 基础语法与核心概念 | 基础语法与数据类型 | 数据类型分类 | 基本数据类型 | 待执行 | 0% | agent-1 | |
   | 第1阶段：[语言]基础 | 基础语法与核心概念 | 基础语法与数据类型 | 数据类型分类 | 引用数据类型 | 待执行 | 0% | agent-2 | |
   ```
   > 灵活适配：简单知识点可合并部分列，保持表格简洁。

**关键要求**：完成规划后，必须先向用户总结计划并请求确认，绝对禁止自动跳转执行。用户确认后将状态更新为「待执行」。

## 模式 2：执行模式（生成学习内容时）
此模式要求输出目录下必须存在已建好的计划文件：
- `@output/plan-learning.md` - 学习计划主体
- `@output/plan-progress.md` - 进度追踪文件

如果这些文件缺失，请立即自动切换回 **模式 1：规划**，先完成计划文件的生成。

### 步骤 2.1：加载上下文与任务分发
1. 读取 `plan-learning.md` 和 `plan-progress.md`
2. 识别当前阶段、模块、章节和待执行的知识点任务
3. 加载对应语言插件的配置和规范
4. 任务粒度适配：
   - 对于简单知识点：单个子 agent 负责完整的知识点
   - 对于复杂知识点：可拆分多个子任务，由不同子 agent 负责不同子知识点
5. 根据 `parallel-agents` 配置，将任务分发给多个子 agent 并行执行
6. 确保任务之间无依赖，避免循环等待

### 步骤 2.2：子 Agent 任务执行
每个子 agent 执行以下标准化流程，生成指定知识点的内容：
#### 2.2.1 知识点文档生成
遵循对应语言插件中的文档结构规范，生成包含以下内容的学习文档：
- 概念讲解和核心特性
- 实际使用场景说明
- 可运行的代码示例（带详细注释）
- 最佳实践和常见坑点
- 相关面试题和解答思路

#### 2.2.2 单元测试生成
生成配套的对应语言的单元测试用例：
- 覆盖核心用法、边界场景、异常情况
- 遵循对应语言的编码规范和测试框架最佳实践
- 测试用例可以直接运行，无额外依赖

#### 2.2.3 内容自校验
每个子 agent 完成内容生成后，必须进行自校验：
- [ ] 文档结构符合对应语言的规范
- [ ] 代码示例可以正常编译运行
- [ ] 测试用例全部可以通过
- [ ] 内容准确无误，没有概念错误

### 步骤 2.3：结果汇总与质量检查
主 agent 收集所有子 agent 的输出结果，进行统一质量检查：
1. **格式检查**：所有文档和测试文件符合统一的格式规范
2. **内容检查**：知识点讲解准确，代码示例正确
3. **一致性检查**：相关知识点之间的表述一致，没有矛盾
4. **完整性检查**：所有计划中的内容都已生成，没有遗漏

如果发现内容缺陷，将任务退回给对应的子 agent 进行修正。

### 步骤 2.4：更新执行进度
每完成一个知识点的生成和校验后，立即更新 `@output/plan-progress.md` 文件：
1. 更新进度表格中的状态和完成度
2. 更新 Metadata 中的时间戳和当前阶段
3. 如果所有任务完成，将状态更新为「已完成」

### 步骤 2.5：阶段迭代
完成当前阶段的所有任务后：
1. 向用户汇报当前阶段完成情况
2. 请求确认是否继续下一阶段
3. 获得确认后，加载下一阶段任务继续执行

## 多 Agent 协作机制
### 任务分发策略
- 按知识点独立性分配任务，避免依赖冲突
- 每个 agent 同一时间只处理一个任务
- 复杂知识点分配给经验更丰富的 agent（通过调整 prompt 实现）

### 结果汇总规范
- 所有文档统一保存到 `@output/docs/` 目录，文件名：`[知识点名称].md`
- 所有测试用例统一保存到 `@output/src/test/[语言]/` 目录，文件名遵循对应语言规范
- 所有代码示例统一保存到 `@output/src/main/[语言]/` 目录，文件名遵循对应语言规范

### 冲突解决机制
- 如果多个 agent 的输出存在内容冲突，由主 agent 进行仲裁
- 优先参考官方文档和权威资料进行修正
- 重大调整需要记录到 `Adjustment requests` 中

## 质量检查清单
在宣布整个学习计划完成之前，必须确认：
- [ ] 所有计划中的知识点都已生成对应的文档和测试
- [ ] 所有代码示例都可以正常编译运行
- [ ] 所有单元测试用例都可以通过
- [ ] 文档结构统一，格式规范
- [ ] 知识点讲解准确，没有概念错误
- [ ] 包含足够的实际案例和最佳实践
- [ ] 进度追踪文件已全部更新完毕

## 工具使用指南
### 并行执行优化
当需要生成多个知识点内容时，必须在同一次回复中**并发（Parallel）**触发多个子 agent 任务，以此达到最高的性能表现：
```typescript
// 示例：并行触发3个子agent任务
task(subagent_type="deep", run_in_background=true, load_skills=[], description="生成Python集合框架文档", prompt="...")
task(subagent_type="deep", run_in_background=true, load_skills=[], description="生成Go并发编程文档", prompt="...")
task(subagent_type="deep", run_in_background=true, load_skills=[], description="生成PHP面向对象文档", prompt="...")
```

### 结果收集
使用 `background_output` 工具收集并行执行的子 agent 结果，不要同步等待任务完成。

### 项目结构生成
使用对应语言插件中的项目结构生成脚本：
```bash
./language-plugins/[语言]/scripts/generate_project_structure.sh <项目名称>
```